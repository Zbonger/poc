pipeline {
    /* insert Declarative Pipeline here */

    //Specify agent
    agent any

    stages {
        stage('Print Environment Variables') {
        steps {
            echo 'ENV'
            sh 'printenv'
          }
        }
    }
}